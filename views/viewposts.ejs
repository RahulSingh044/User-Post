<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body class="bg-gray-100 min-h-screen p-5">
    <a href="/profile" class="text-3xl"><i class="fa-solid fa-arrow-left"></i></a>
    <div class="posts mt-6">
        <h2 class="text-black text-2xl font-bold">Your Posts</h2>
        <hr class="text-2xl">
        <% user.post.forEach(ele => { %>
            <div class="postcontainer mt-4 grid grid-cols-3 gap-5">
                <div class="post mb-5  bg-white p-3 rounded-lg shadow-lg">
                    <h4 class="font-bold  text-blue-500 mb-2">@<%= user.username  %></h4>
                    <p class="text-sm tracking-tight"><%= ele.content %></p>
                    <div class="btns flex  mt-2">
                        <!-- Initially heart should be black when user click on heart it should turn pink -->
                        <a href="/like/<%= ele._id %>" class="text-lg text-black" ><i class="fa-solid fa-heart"></i></a><p class="text-lg ml-1"><%= ele.likes.length %></p>
                        <a href="/edit/<%= ele._id%>" class=" ml-4 text-lg text-black"><i class="fa-solid fa-pen-to-square"></i></a>
                    </div>
                </div>
            </div>
        <% }); %>
        
    </div>
</body>

</html>